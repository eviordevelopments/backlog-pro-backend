# Backlog Pro Backend - Ejemplo de Docker Compose
# Copia este archivo a .env y actualiza los valores según tu entorno
# Usado con docker-compose.yml para ejecutar localmente con imagen de DockerHub

# ============================================================
# Configuración del Servidor
# ============================================================
NODE_ENV=development
PORT=3000

# ============================================================
# Configuración de la Base de Datos (PostgreSQL)
# ============================================================
# OPCIÓN 1: Usando docker-compose.yml (recomendado para desarrollo local con Docker)
#   - DB_HOST debe ser "postgres" (nombre del servicio Docker)
#   - Usuario por defecto es "postgres" con contraseña "postgres"
#   - Comando: docker-compose up
#
# OPCIÓN 2: PostgreSQL Local (sin Docker)
#   - DB_HOST debe ser "localhost" (tu máquina)
#   - DB_USERNAME y DB_PASSWORD deben coincidir con tu usuario local de PostgreSQL
#   - DB_DATABASE debe coincidir con una base de datos que creaste en PostgreSQL
#   - Crear base de datos: createdb backlog_pro
#   - Comando: npm run start:dev
#
# OPCIÓN 3: Producción en Render
#   - Usar la URL interna de PostgreSQL de Render
#   - DB_HOST, DB_USERNAME, DB_PASSWORD del panel de Render
#   - DB_SSL debe ser "true" para conexión segura
# ============================================================

# Configuración Local/Docker
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=backlog_pro

# Alternativa: Para producción en Render, usar DATABASE_URL en lugar de esto:
# DATABASE_URL=postgresql://postgres:PASSWORD@HOST:5432/backlog_pro

# ============================================================
# Configuración JWT
# ============================================================
JWT_SECRET=change-this-to-a-secure-random-string-in-production
JWT_EXPIRES_IN=24h

# ============================================================
# Configuración de Docker Hub (para docker-compose.yml)
# ============================================================
# Reemplaza con tu usuario real de DockerHub
DOCKER_USERNAME=your-dockerhub-username
